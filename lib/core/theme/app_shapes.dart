import 'package:flutter/material.dart';

class AppShapes {
  const AppShapes._();

  static const card = BorderRadius.all(Radius.circular(34));
  static const button = BorderRadius.all(Radius.circular(28));
  static const input = BorderRadius.all(Radius.circular(14));
  static const chip = BorderRadius.all(Radius.circular(12));
  static const sheet = BorderRadius.vertical(top: Radius.circular(28));
}
